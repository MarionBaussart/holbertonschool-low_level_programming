#include <stdio.h>
/**
 * main - Every C co must be contain main
 * void = vide
 * Return: 0 if its work
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

